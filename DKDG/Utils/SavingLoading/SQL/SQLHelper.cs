namespace DKDG.Utils.SavingLoading.SQL
{
    public static class SQLHelper
    {
        //using (var conn = new SQLiteConnection(@"Data Source=D:\test.db;"))
        //{
        //    conn.Open();

        // var command = new SQLiteCommand("Select * from yourTable", conn); SQLiteDataReader reader
        // = command.ExecuteReader();

        // SQLiteCommand cmd = _connection.CreateCommand(); cmd.CommandType = CommandType.Text;
        // cmd.CommandText = "SELECT * FROM MyTable WHERE MyColumn = @parameter";
        // cmd.Parameters.Add(new SQLiteParameter("@parameter", textfield)); SQLiteDataReader reader
        // = cmd.ExecuteReader();

        // while (reader.Read()) Console.WriteLine(reader["YourColumn"]);

        //    reader.Close();
        //}
    }
}
